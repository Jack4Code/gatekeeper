version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: gatekeeper-db
    environment:
      POSTGRES_DB: authdb
      POSTGRES_USER: authuser
      POSTGRES_PASSWORD: authpass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # Note: Migrations are now managed by bedrock-migrate
      # Run manually after startup: docker-compose exec gatekeeper ./gatekeeper migrate up --type postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U authuser -d authdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  gatekeeper:
    build: .
    container_name: gatekeeper
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      DATABASE_URL: postgres://authuser:authpass@postgres:5432/authdb?sslmode=disable
      HTTP_PORT: 8080
      HEALTH_PORT: 9090
    ports:
      - "8080:8080"
      - "9090:9090"
    restart: unless-stopped

volumes:
  postgres_data: